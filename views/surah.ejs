<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= surah.name_simple %> - Hikmah Verse</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1>Hikmah Verse</h1>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/list-surah">Al-Qur'an</a></li>
                <li><a href="/jadwal-sholat">Jadwal Sholat</a></li>
            </ul>
        </nav>
    </header>
    <main>
        <h2><%= surah.name_arabic %> (<%= surah.name_simple %>)</h2>
        <p><strong>Jumlah Ayat:</strong> <%= surah.verses_count %></p>
        <p><strong>Tempat Turun:</strong> <%= surah.revelation_place %></p>
        <p><strong>Terjemahan Nama:</strong> <%= surah.translated_name.name %></p>

        <h3>Ayat-ayat dengan Terjemahan:</h3>
        <ul>
            <% if (verses && translations && verses.length > 0 && translations.length > 0) { %>
                <% verses.forEach((verse, index) => { %>
                    <li>
                        <p><strong>Ayat <%= verse.verse_key.split(':')[1] %>:</strong></p>
                        <p><em>Teks Arab:</em> <%= verse.text_uthmani %></p>
                        <p><em>Terjemahan:</em> <%= translations[index] ? translations[index].text.replace(/<sup[^>]*>.*?<\/sup>/g, '') : 'Terjemahan tidak tersedia' %></p>
                    </li>
                <% }); %>
            <% } else { %>
                <p>Tidak ada ayat atau terjemahan yang tersedia untuk surah ini.</p>
            <% } %>
        </ul>
    </main>
</body>
</html>
